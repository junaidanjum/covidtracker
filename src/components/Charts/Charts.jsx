import React from 'react';
// import  {fetchDaily} from "../../api/index.js"

const Charts = (props) => {
    console.log(props);
    return (
        <div>
            Charts
        </div>
    );
};

export default Charts;